using UnityEngine;

//this script is used for when the gun uses the role as a pick up
public class GunInteractable : MonoBehaviour, IIinteractable
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
    public void Interact()
    {

    }
}
